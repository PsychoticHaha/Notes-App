# Don't forget to change credentials inside pdo.php file to your own database connection.

{
  $dbUsername = 'change1';
  $dbPassword = 'change2';
  $dsn = 'mysql:host=change3;dbname=change4';
}
** change1 should be changed to "Database Username" of your host service;
** change2 should be changed to "Database Password" of your host service;
** change1 should be changed to "Database Host Server" of your host service;
** change1 should be changed to "Database Name" of your host service;



